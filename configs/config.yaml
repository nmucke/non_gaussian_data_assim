defaults:
  - _self_
  - case: lorenz_63
  - da_method: enkf

# Common, case-agnostic settings.
seed: 42

# Sensible fallbacks; each case file overrides these to match its needs.
outer_steps: 100
inner_steps: 50
ensemble_size: 100

hydra:
  output_subdir: null
  run:
    dir: .
